const Resume = ({name, surname, email, mobile}) => {
    return (
        <main>
            <p>Name: {name}</p>
            <p>Surname: {surname}</p>
            <p>Email: {email}</p>
            <p>Mobile: {mobile}</p>
        </main>
    )
}

export default Resume